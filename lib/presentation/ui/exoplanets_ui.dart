import 'package:flutter/material.dart';

class ExoPlanetsUI extends StatelessWidget {
  const ExoPlanetsUI({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Nasa Exoplanet Tracker"),
    );
  }
}
